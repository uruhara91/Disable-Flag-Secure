# Zygisk Secure Capture configuration
# Accepted values: true/false, yes/no, on/off, 1/0.
# Features without compiled backends remain disabled by default.

capture_secure_layers=true
metadata_sanitizer=false
block_screenshot_detection=false
block_screen_recording_detection=false
legacy_fallback=false
vendor_adapters=false
debug=false
